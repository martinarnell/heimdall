# Build artifacts
/target
.DS_Store

# ── Environment (secrets) ───────────────────────────────────────────
.env

# ── Data downloads (large, user-specific) ─────────────────────────────
/data/osm/
/data/downloads/
/data/photon/
/data/photon-gb/
/data/denmark/
/data/norway/
/data/lm_*/

# ── Built indices (generated by heimdall-build) ───────────────────────
/data/index-*/

# ── Rebuild pipeline state ────────────────────────────────────────────
data/rebuild-state.json
data/rebuild-report-*.log

# ── Intermediate build artifacts ──────────────────────────────────────
*.parquet
*.pbf
*.zip
*.tar.bz2

# ── Benchmark / comparison artifacts ──────────────────────────────────
/data/benchmarks/
data/bench-results*.csv
data/bench-queries.txt
data/lm_urls.txt

# ── Development notes (internal, not for release) ────────────────────
/notes/

# ── Release packages ─────────────────────────────────────────────────
/dist/

# ── Rebuild logs ─────────────────────────────────────────────────────
rebuild*.log

# ── Global data cache ────────────────────────────────────────────────
/data/global/

# ── Stray config copies ──────────────────────────────────────────────
/sv.toml

# ── Local dev notes ──────────────────────────────────────────────────
/CLAUDE.local.md
